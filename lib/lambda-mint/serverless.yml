service: lambda-mint
frameworkVersion: "2 || 3"

provider:
  name: aws
  runtime: nodejs14.x
  lambdaHashingVersion: "20201221"

functions:
  main:
    handler: index.handler
    environment:
      ETH_FROM_ADDRESS: "0xADDRESS"
      ETH_PRIV_KEY: "private_key"
